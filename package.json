{
  "name": "diem-danh-hn2-soc",
  "version": "0.1.0",
  "private": true,
  "description": "Điểm Danh HN2 SOC — GAS WebApp điểm danh kiểm-soát có/vắng/trễ cho kho chuyển phát nhanh",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:py": "python3 -m unittest discover -s api -p 'test_*.py'",
    "test:chrome": "node scripts/test-local-mock.js",
    "test:chrome:mock": "node scripts/test-local-mock.js",
    "dev": "node scripts/serve.js",
    "build": "node scripts/build-static.js",
    "build:local": "node scripts/build-local.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "ws": "^8.21.3"
  }
}
